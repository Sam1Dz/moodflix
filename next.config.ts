import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: true // Set to 'false' if you want to try Development like Production mode
};

export default nextConfig;
